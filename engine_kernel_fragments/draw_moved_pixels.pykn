draw_moved_pixels
9

        struct color tempColor = resources->substanceTable->substances[thisVoxelCopy.substanceID].color;
        write_imagef(texture, (int2)(tempX, tempY), (float4)(tempColor.R, tempColor.G, tempColor.B, 1.0f));

